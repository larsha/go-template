version: '2'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    command: go run main.go
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./app:/go/src/app
