version: '2'
services:
  redis:
    image: redis:latest
  web:
    build:
      context: .
      dockerfile: Dockerfile.web.dev
    ports:
      - "3000:3000"
    volumes:
      - ./web:/go/src/app
    depends_on:
      - redis
