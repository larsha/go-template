version: '2'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web.dev
    command: go run main.go utils.go
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - ./web:/web
